'use strict';

module.exports = {
    routes: [ //custom routes
        {
            method: 'GET', 
            path: '/adminPromo',
            handler: 'news-promotion.adminfind'
        },
    ]
}
